{
  "name": "staysphere-root",
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm install",
    "postbuild": "mkdir -p backend/dist && cp -rv frontend/dist/staysphere-frontend/* backend/dist/",
    "build": "npm run install:all && npm run build:frontend && npm run postbuild"
  },
  "dependencies": {
    "bootstrap-icons": "^1.13.1"
  }
}
